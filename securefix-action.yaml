# yaml-language-server: $schema=https://raw.githubusercontent.com/csm-actions/securefix-action/refs/tags/v0.2.0/json-schema/config.json
entries:
  - client:
      repositories:
        - csm-actions/demo-client
      branches:
        - main
    push:
      repositories:
        - csm-actions/demo-client
      branches:
        - test-*
    pull_request:
      base_branches:
        - main
